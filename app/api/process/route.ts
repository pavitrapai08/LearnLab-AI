// Extract text from a Storage path or pasted text (P1). maxDuration=60; derive auth.uid() from verified token; reject storagePath outside {uid}/. TECH_SPEC 3.1/5.

export const maxDuration = 60

export async function POST() {
  return new Response('Not implemented', { status: 501 })
}

